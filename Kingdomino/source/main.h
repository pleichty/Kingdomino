#pragma once

void pickTile(SDL_Renderer * renderer, SDL_Texture * bg_texture, int  tileNumbers[48], int tileCounter, SDL_Texture * textures[20], bool &decision_made, u32 &kdown, GameStateManager &gameStateManager);
